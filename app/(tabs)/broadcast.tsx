import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Broadcast = () => {
  return (
    <SafeAreaView>
      <View className="">
        <Text className=" text-xl font-SF_Medium">
          This is the Braodcast Page
        </Text>
      </View>
    </SafeAreaView>
  );
};

export default Broadcast;
