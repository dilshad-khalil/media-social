import React from "react";
import UserProfile from "../user-profile/profile";

const Profile = () => {
  return (
    <>
      <UserProfile hasHeader={false} />
    </>
  );
};

export default Profile;
